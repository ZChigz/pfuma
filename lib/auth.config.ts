// Auth callbacks and Credentials provider are consolidated in lib/auth.ts.
// This file is kept as a stub for any legacy imports.
import type { NextAuthConfig } from 'next-auth';

export const authConfig: NextAuthConfig = {
  providers: [],
};
